# Default values for aks-mcp.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: ghcr.io/azure/aks-mcp
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# Application configuration
app:
  # Transport mechanism: stdio, sse, streamable-http
  transport: "streamable-http"
  # Port to listen on
  port: 8000
  # Access level: readonly, readwrite, admin
  accessLevel: "readonly"
  # Command execution timeout in seconds
  timeout: 600
  # Log level: debug, info, warn, error
  logLevel: "info"
  # Enable cache for better performance
  cache: true
  # Enable token-only authentication mode for supported tools
  # When enabled, tools like kubectl use Azure AKS RunCommand API with user tokens
  # Requires transport to be sse or streamable-http (not compatible with stdio)
  tokenAuthOnly: false

# Azure authentication configuration
azure:
  # Use existing secret for Azure credentials (for Service Principal auth)
  existingSecret: ""
  # Azure tenant ID (can be set via secret for Service Principal)
  tenantId: ""
  # Azure client ID (can be set via secret for Service Principal)
  clientId: ""
  # Azure client secret (only for Service Principal auth, ignored when Workload Identity is enabled)
  clientSecret: ""
  # Azure subscription ID to set the current default active subscription
  subscriptionId: ""

# Azure Workload Identity configuration
workloadIdentity:
  # Enable Azure Workload Identity for passwordless authentication
  # When enabled, requires azure.tenantId and azure.clientId to be set
  # Federated credential must be created in Azure before installation
  enabled: false

# OAuth configuration
oauth:
  # Enable OAuth authentication
  enabled: false
  # Azure AD tenant ID for OAuth (falls back to azure.tenantId)
  tenantId: ""
  # Azure AD client ID for OAuth (falls back to azure.clientId)
  clientId: ""
  # Custom OAuth scopes (e.g., ["api://your-app-id/.default"])
  # If empty, defaults to https://management.azure.com/.default
  # Use custom App ID URI scope to enforce "Assignment Required" in Azure AD
  scopes: []
  # Custom redirect URIs (auto-generated if empty)
  redirectURIs: []
  # Custom CORS origins (auto-generated if empty)
  corsOrigins: []
  # External base URL of the server (e.g. https://aks-mcp.example.com).
  # Required when the server is behind a TLS-terminating reverse proxy so that
  # OAuth metadata endpoints return https:// URLs instead of http://.
  externalURL: ""
  # Enable On-Behalf-Of token exchange so tokenAuthOnly tools run as the calling user.
  # Requires azure.clientSecret (or azure.existingSecret with a client-secret key) to be set.
  oboEnabled: false

# HTTP transport security: DNS-rebinding / cross-origin protection.
# Applies to streamable-http and sse transports only; stdio is unaffected.
#
# The chart deployment hard-codes --host 0.0.0.0 so the listener is always
# routable inside the cluster. When app.transport is streamable-http or sse
# and oauth.enabled is false, the server REFUSES to start unless
# security.allowedHosts is set. Pick one of:
#   (a) enable OAuth (oauth.enabled: true), or
#   (b) declare an explicit trusted-host allowlist below.
# A default install with neither set will CrashLoopBackOff on purpose.
security:
  # HTTP Host header values to accept on /mcp, /sse and /message.
  # Entries may include a port (e.g. "aks-mcp.example.com:8000") to match
  # only that port, or omit it to match any port for that hostname.
  # Use the literal "*" only when an upstream reverse proxy validates Host
  # on the operator's behalf.
  # Example: ["aks-mcp.internal.example.com"]
  allowedHosts: []
  # HTTP Origin header values to accept on browser-style cross-origin requests
  # to /mcp, /sse and /message. Empty Origin (non-browser clients) always allowed.
  # Use the literal "*" only to disable Origin enforcement entirely.
  # Example: ["https://chat.internal.example.com"]
  trustedOrigins: []

# Application configuration
config:
  # Enabled components (empty means all components enabled)
  # Available components: az_cli, monitor, fleet, network, compute, detectors, advisor, inspektorgadget, kubectl, helm, cilium, hubble
  enabledComponents: []
  # Allowed Kubernetes namespaces (empty means all)
  allowNamespaces: []
  # Cache timeout
  cacheTimeout: "60s"

# Kubeconfig configuration
kubeconfig:
  enabled: false
  secretName: "kubeconfig"

# Telemetry configuration
telemetry:
  # OTLP endpoint for OpenTelemetry traces
  otlpEndpoint: ""

# Durable MCP Task storage. When enabled, AKS-MCP exposes task polling and
# cancellation only for the read-only aks_cluster_health tool. Use a ReadWriteOnce
# claim and keep replicas at one unless replacing this file store with shared
# storage. An emptyDir is deliberately not offered because pod replacement
# would break a task handle.
taskStore:
  enabled: false
  existingClaim: ""
  mountPath: "/var/lib/aks-mcp/tasks"

# Immutable remediation plans and external approval records. This is separate
# from MCP Task state so a triage server cannot accidentally turn a task PVC
# into a mutation authority. Enable only for the explicitly requested
# remediation component and use the same PVC in the restricted apply workflow.
remediationStore:
  enabled: false
  existingClaim: ""
  mountPath: "/var/lib/aks-mcp/remediation"

# Ingress configuration
ingress:
  # Enable ingress for the application
  enabled: false

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# RBAC settings
rbac:
  # Specifies whether RBAC resources should be created
  create: true

# Pod security settings
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  capabilities:
    drop:
      - ALL

service:
  type: ClusterIP
  port: 8000

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

livenessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 5
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 3

# Extension points for advanced usage
extraVolumes: []
extraVolumeMounts: []
extraEnv: []

# Optional Kubernetes scheduling
podLabels: {}
podAnnotations: {}
nodeSelector: {}
tolerations: []
affinity: {}
